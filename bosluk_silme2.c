#include<stdio.h>
int main(void){
    char cumle;
    int bosluk;
    bosluk='\0';
    printf("cumle giriniz: ");
    scanf("%c",cumle);
    for( ; ; )
         if cumle[i]=bosluk {
            if cumle[i+1]=bosluk
                cumle[i+1]='';
            else
                i++;}
    getch();
    return 0;
} 
